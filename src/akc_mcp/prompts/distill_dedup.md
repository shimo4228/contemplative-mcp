For each NEW pattern below, compare it against the EXISTING candidates and decide:
- ADD: genuinely new insight, not covered by any candidate
- UPDATE N: same core lesson as candidate N, just different wording (merge into existing)
- SKIP: already fully covered by a candidate, adds nothing new

Focus on MEANING, not wording. Two patterns expressing the same lesson in different words = UPDATE.

Return JSON: {{"decisions": ["ADD", "UPDATE 1", "SKIP", ...]}}
One decision per NEW pattern, in order.

{dedup_items}
